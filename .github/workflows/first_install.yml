name: First Install

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  first_install:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: known_hosts (port 22)
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p 22 -H "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts

      - name: Install Ansible
        run: |
          sudo apt-get update
          sudo apt-get install -y ansible
          ansible-galaxy collection install community.general

      - name: inventory.ini (port 22)
        run: |
          cat > ansible/inventory.ini <<'EOF'
          [servers]
          ${{ secrets.SSH_HOST }} ansible_user=${{ secrets.SSH_USER }} ansible_port=22
          EOF

      - name: group_vars/all.yml (from secrets)
        run: |
          mkdir -p ansible/group_vars
          cat > ansible/group_vars/all.yml <<EOF
          app_domain: "${{ secrets.APP_DOMAIN }}"
          letsencrypt_email: "${{ secrets.LETSENCRYPT_EMAIL }}"
          ssh_port: ${{ secrets.SSH_PORT }}
          app_replicas: ${{ secrets.APP_REPLICAS }}
          mariadb_image: "${{ secrets.MARIADB_IMAGE }}"
          k9s_version: "${{ secrets.K9S_VERSION }}"
          app_image: "ghcr.io/placeholder/placeholder:first_install"
          mariadb_root_password: "${{ secrets.MARIADB_ROOT_PASSWORD }}"
          mariadb_database: "${{ secrets.MARIADB_DATABASE }}"
          mariadb_user: "${{ secrets.MARIADB_USER }}"
          mariadb_password: "${{ secrets.MARIADB_PASSWORD }}"
          EOF

      - name: Run bootstrap.yml
        run: |
          cd ansible
          ansible-playbook -i inventory.ini bootstrap.yml
